commands:
  # Default for all commands - require confirmation
  execute_command:
    pattern: 'execute_command'
    supervision: confirm
    risk: medium
    safety_checks:
      - command_validator
      - sandbox
